const UnlockPort = {
    label: 'unlock',
    group: 'in'
};

const AvailableConditionPort = {
    label: 'available',
    group: 'in'
};

const UnlockedPort = {
    label: 'unlocked',
    group: 'out'
};

const AvailablePort = {
    label: 'available',
    group: 'out'
};

const CompletedPort = {
    label: 'completed',
    group: 'out'
};

const LogicalInPort = {
    label: 'logical in',
    group: 'in'
};

const LogicalOutPort = {
    label: 'logical out',
    group: 'out'
};

const DelayInPort = {
    label: 'delay in',
    group: 'in'
};

const DelayOutPort = {
    label: 'delay out',
    group: 'out'
};

export { 
    UnlockPort, 
    AvailableConditionPort, 
    UnlockedPort, 
    AvailablePort, 
    CompletedPort,
    LogicalInPort,
    LogicalOutPort,
    DelayInPort,
    DelayOutPort
};