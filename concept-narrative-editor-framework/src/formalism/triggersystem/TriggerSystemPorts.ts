const UnlockPort = {
    name: 'unlock',
    group: 'in'
};

const AvailableConditionPort = {
    name: 'available',
    group: 'in'
};

const UnlockedPort = {
    name: 'unlocked',
    group: 'out'
};

const AvailablePort = {
    name: 'available',
    group: 'out'
};

const CompletedPort = {
    name: 'completed',
    group: 'out'
};

const LogicalInPort = {
    name: 'logical in',
    group: 'in'
};

const LogicalOutPort = {
    name: 'logical out',
    group: 'out'
};

const DelayInPort = {
    name: 'delay in',
    group: 'in'
};

const DelayOutPort = {
    name: 'delay out',
    group: 'out'
};

export { 
    UnlockPort, 
    AvailableConditionPort, 
    UnlockedPort, 
    AvailablePort, 
    CompletedPort,
    LogicalInPort,
    LogicalOutPort,
    DelayInPort,
    DelayOutPort
};