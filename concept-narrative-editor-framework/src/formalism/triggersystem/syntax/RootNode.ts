import TriggerSystemNode from '../TriggerSystemNode';

class RootNode extends TriggerSystemNode {
    constructor(attributes?: any, options?: any) {
        super(attributes, options);
    }
}

export default RootNode;